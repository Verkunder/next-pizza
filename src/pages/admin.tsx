import React from 'react';
import Head from 'next/head';

const Admin = () => {
    return (
        <div>
            <Head>
                <title>Pizza</title>
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <h1>Hello, world!</h1>
        </div>
    );
};

export default Admin;
